//datatype union y can have more than two  unions i.e number | string | boolean
const combine = (input1: number | string ,input2: number | string)=>{

    const result = input1 + input2
    return result
}